server:
  port: 9991

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userdb
    username: root
    password: mysql@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
